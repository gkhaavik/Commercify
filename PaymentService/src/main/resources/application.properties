spring.application.name=payment-service
server.port=8082
stripe.api.secret-test-key=${STRIPE_TEST_SECRET:sk_missing}
stripe.webhook.secret=${STRIPE_SECRET_WEBHOOK:wh_missing}
stripe.webhook.endpoint=${STRIPE_WEBHOOK_ENDPOINT:we_missing}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/paymentdb
spring.datasource.username=root
spring.datasource.password=${LOCAL_DATABASE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
order.service.url=http://localhost:8081/api/v1/orders
